# Norway Open Data Pipeline

A production-style data pipeline that extracts public Norwegian statistics from SSB, transforms and validates the data, loads it into PostgreSQL, and orchestrates the workflow with Apache Airflow.

Initial dataset target: population statistics by municipality and year.
